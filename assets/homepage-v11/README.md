# v11 日本語校正 — 2026-09-22

対象は総合HP homepage-world.html の全セクション（トップ、サービス、自己紹介、制作実績、相談案内、ナビ、メタ情報、画像説明）。制作実績のリンク先にある別サイトや過去のデザイン版は対象外。

## 実行経路

本人指定の Antigravity CLI `~/.local/bin/agy` を使用。以前の実測手順を確認後、現行 --help / models を取得。空の作業フォルダで、今回のHTMLと校正条件だけを渡した。

指定モデル: `--model gemini-3.1-pro --effort high`。`--output-format json --disable-slash-commands --print-timeout 240s`。APIキー経路や別モデルへの代替なし。初回と修正後再確認の2回がexit 0 / SUCCESS。出力に実行モデル名の独立したフィールドはないため、上記はCLI指定値として記録。

初回: 9提案。2件をそのまま、4件を編集して採用し、短い作品キャプション3件は原文を保持。全文再確認: ready / issues 0。再確認後に、スマホで「す。」だけが孤立した制作実績の紹介文へ読点と改行を追加（意味の変更なし）。詳細はreview.json / decisions.json / final-review.json。

- 検索向け説明文を自然な文章にする。
- ストレングスの説明で何を読み解くかを明示し、述語を補う。
- 自己紹介の「記録も、悩みも」に述語を補い、提案に含まれた「負担」は追加しない。
- 制作実績の見出しをナビと揃え、紹介文に述語を補う。
- 「何も決まっていない」でも大丈夫 → まだ何も決まっていなくても大丈夫です。

キャッチコピー・固有名詞・16年の経歴・相談条件・リンクを保持。「Miuzno lab.」はリンク先HTMLのtitle/見出し/ロゴにも同表記があるため、推測で訂正しない。

## 親側の確認

元HTMLとの差分は6か所。HTMLParserでmeta descriptionと追加brを除いたタグ・属性列が一致（画像、リンク、class、idを保持）。CSS/JS変更なし。git diff --check通過。

IABで保存後の日本語を読み戻し、PC幅1248pxの自己紹介、スマホ幅390pxのサービス・制作実績・相談案内を目視。横はみ出しなし（1248/scrollWidth1233、390/scrollWidth375）、画像欠損0、テキスト要素の内部横あふれ0。孤立した「す。」は改行調整後の画面で解消を確認。

これは日本語とレイアウトの確認であり、料金・経歴等の外部事実確認ではない。公開mainはv8、校正版は比較ブランチに保存するv11。
